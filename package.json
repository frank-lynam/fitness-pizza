{
  "name": "fitness-pizza",
  "version": "2.3.7",
  "description": "Fitness Pizza — macro & workout tracker PWA with native run tracking",
  "private": true,
  "scripts": {
    "prepare-web": "rm -rf www && mkdir -p www && cp index.html manifest.json sw.js www/ && cp -r css js img www/",
    "sync:android": "npm run prepare-web && npx cap sync android",
    "sync:ios": "npm run prepare-web && npx cap sync ios",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor-community/background-geolocation": "^1.2.4",
    "@capacitor-community/text-to-speech": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
